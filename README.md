# JiraVersionChecker
Check jira issues in a specific version of a project

Usage ./version_checker <project> <version>

Para que funcione hay que crear un archivo `credentials.json` en el mismo directorio del ejecutable con el formato

```
{
  "user": "UserName",
  "password": "Password"
}
```
